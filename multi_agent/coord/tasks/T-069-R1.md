# TASK T-069-R1

OWNER: w1

## Goal

- Fix soils.sol semantics: current calibration.cal tail carries HRU IDs instead of soil/profile object IDs. This is invalid.

## Problem

- T-069 opened soils.sol filter support by reusing HRU object-id path.
- Calibration.cal OBJ_TOT tail for soils.sol params currently filled with HRU IDs.
- SWAT+ calibration expects soil/profile object IDs when ob_typ=sol, not HRU IDs.
- No stable soil profile ID mapping exists in current codebase (soils.sol parsing not implemented).

## Required

- **Option A** (preferred): Implement true soil/profile object IDs from project files.
  - Parse soils.sol to build soil profile ID mapping.
  - Resolve soil-name filter to actual soil profile indices.
  - HRU IDs must NOT appear in soil calibration records.
- **Option B** (fallback): Revert soils.sol filter support to rejected-at-validation.
  - Remove soils.sol from filterable source files.
  - Restore clear rejection: "soils.sol does not yet support object-space targeting."
  - Remove false "implemented" claim.
- Keep bsn/hru/cha status unchanged. Do not touch cha unless tests require it.

## Scope

- Fix: builder.py, validate.py, template.py as needed.
- Update tests for whichever option is implemented.
- Update docs to match final status.
- Prefer correctness over breadth.

## Done When

- soils.sol calibration records carry correct object IDs (A), OR soils.sol filter is rejected (B).
- Tests pass.
- Final soils.sol status clearly stated.
