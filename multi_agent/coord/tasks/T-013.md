# TASK T-013

OWNER: w3

## Goal

- 为 `swatplus` 第一阶段设计一份验证清单，覆盖模板校验、IO 协议、端到端三层验证。
- 输出一份“实现即验证”的测试规划，供后续开发同步落地。

## Must Know Before Execution

- 这轮任务以验证设计为主，不修改代码。
- 验证目标是 `swatplus` MVP，不是完整 SWAT+ 支持。
- 第一阶段范围：`TxtInOut + txt/csv 输出 + 少量参数 + 少量输出对象`。
- 要贴合当前测试组织方式，不要另起体系。

## Scope

- 阅读并分析：
- `tests/`
- `tests/test_validate.py`
- `tests/test_monthly_complex_io_protocol.py`
- `tests/test_runtime_behavior_locks.py`
- `examples/`
- 输出：
- 模板级验证点
- IO 协议验证点
- 端到端验证点
- 推荐夹具形态

## Out Of Scope

- 不修改代码。
- 不创建 fixture。
- 不写测试实现。

## Done When

- 给出一份三层验证清单。
- 至少包含：
- 每层验证目标
- 推荐测试文件落位
- 最小 fixture 要素
- 哪些风险必须通过测试锁住
