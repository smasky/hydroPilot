# TASK T-047

OWNER: w3

## Goal

- 基于当前 `swatplus_db.yaml` / `series.py`，准备一份 `swatplus` series 库补齐方案，重点面向流量抽取场景。

## Must Know Before Execution

- owner 已明确：后面不仅要补系列库，还要让用户理解“怎么像 SWAT2012 一样取站点流量”。
- 当前 series 库主要还是 `aa`，明显不够。
- 这次先做方案，不直接大规模改库。

## Scope

- 结合现有代码结构，整理：
  - 当前 `series.py` 如何按 file/object/frequency/id 工作
  - series DB 如果扩到 `yr / mo / da`，最小应该补哪些字段
  - 流量场景下，推荐先补哪些文件/变量
- 给出一版最小补库计划，方便 leader 下一轮直接派“落库实施”任务。

## Out Of Scope

- 不直接改 `swatplus_db.yaml`
- 不做用户思路调研主结论（那是 T-045）
- 不改 runtime / extractor 代码

## Done When

- 给出一份可执行的 `swatplus` series 扩库计划
- 重点覆盖流量相关的 `yr / mo / da` 场景

