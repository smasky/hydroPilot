# TASK T-034

OWNER: w2

## Goal

- 为 hydroPilot 设计并尽量实现一个通用 `formatted_text` writer 最小骨架。

## Must Know Before Execution

- owner 已确认：`formatted_text` 可以作为新的通用 writer 名称。
- 该 writer 必须是 general 层通用能力，不能夹带 `swatplus` 私货。
- 初始化阶段负责静态骨架，运行阶段负责动态值回填。
- `swat2012` 继续走 `fixed_width`，`xaj` 继续走 `csv`。

## Scope

- 基于现有 writer 体系评估最小接口：
  - 注册方式
  - `validateSpec`
  - `buildSpec`
  - 初始化能力
  - 动态值回填能力
- 优先关注：
  - `src/hydropilot/io/writers/base.py`
  - `src/hydropilot/io/writers/__init__.py`
  - 新增 `src/hydropilot/io/writers/formatted_text.py`
- 如果可行，补最小单元测试，验证：
  - 能初始化静态文本骨架
  - 能对已存在骨架回填动态值

## Out Of Scope

- 不写 `swatplus` 专属 `calibration.cal` 业务规则。
- 不改 runtime 初始化挂点主流程。
- 不扩展 `swatplus` 参数库。

## Done When

- 有一份清楚的最小 writer 设计或代码实现。
- 证明该 writer 从命名、接口、职责上都是通用的。
- 如有测试，至少覆盖初始化与 apply 两种职责分离。

