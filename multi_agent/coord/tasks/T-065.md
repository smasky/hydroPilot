# TASK T-065

OWNER: w1

## Goal

- Wire SWAT+ template validation into real pipeline so fail-fast works during prepare_config/load_config.

## Must Know

- T-063 found: SwatPlusTemplate.validate() inherits ModelTemplate.validate() which returns [].
- validate_swatplus_config() exists but is only called standalone — not through the template pipeline.
- Need SwatPlusTemplate.validate() to call validate_swatplus_config(raw, base_path).

## Scope

- Implement SwatPlusTemplate.validate() calling validate_swatplus_config().
- Add regression test: basin param + filter fails through prepare_config/load_config with intended diagnostic.
- Add test: valid hydrology.hyd filter config passes that stage.
- Keep builder scope gate unchanged.

## Done When

- Filter fail-fast effective at prepare_config/load_config.
- Tests pass.
