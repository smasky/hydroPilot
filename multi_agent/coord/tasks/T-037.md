# TASK T-037

OWNER: w2

## Goal

- 为下一轮 `swatplus_db.yaml` 扩库确认真实项目文件落点，重点覆盖首批高价值分组。

## Must Know Before Execution

- owner 已明确：不是样例里有才支持，而是“源码支持 + 文件落点可确认，就入库”。
- 这次任务重点是**落点层**，不是纯源码支持判断。
- 当前首批优先组建议：`soils.sol`、`channels`、`aquifer`、`reservoir`。

## Scope

- 基于本地可用 SWAT+ 项目样例、已有下载样例、当前仓库中的参考数据，确认：
  - 文件名
  - 行/列结构是否稳定
  - 单值还是多层/多对象
  - 是否适合现有 `fixed_width` 或新 `formatted_text`
- 优先产出：
  - 首批可入库参数候选的真实落点表
  - 对落点不稳定/样例不足的项做明确标注

## Out Of Scope

- 不直接实现大规模知识库写入。
- 不改 runtime 初始化边界。
- 不把高风险不确定参数硬塞进库。

## Done When

- 给出一份可用于实际入库的落点确认表。
- 明确哪些组已经可以开始写库，哪些还缺样例或缺验证。

