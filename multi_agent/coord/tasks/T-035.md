# TASK T-035

OWNER: w3

## Goal

- 为 `swatplus` 整理一份最小模型知识库落地方案，并尽量落实到源码组织上。

## Must Know Before Execution

- owner 已确认：`swatplus` 需要像 `swat2012` 一样有自己的模型知识库。
- 这里的“数据库”指 YAML 知识库，不是运行时数据库。
- 目标是支撑后续：
  - 参数校验
  - builder 翻译
  - `formatted_text`/初始化阶段接线
- 要求克制，不追求一口气全覆盖。

## Scope

- 基于当前 `models/swatplus/` 真实代码，整理最小第一版知识库字段建议。
- 优先关注：
  - `src/hydropilot/models/swatplus/swatplus_db.yaml`
  - `src/hydropilot/models/swatplus/builder.py`
  - `src/hydropilot/models/swatplus/validate.py`
  - 其他现有 swatplus 模块中已经散落的参数事实
- 产出应至少回答：
  - 最小字段集应该有哪些
  - 哪些字段适合进库，哪些不适合
  - 接下来接入 validate / builder 的顺序建议
- 如果能安全改动，可先把知识库结构或注释整理好，但不要大范围重构。

## Out Of Scope

- 不实现 runtime 初始化机制。
- 不实现新的通用 writer。
- 不强行补全所有 SWAT+ 参数。

## Done When

- 给出最小知识库字段集与组织建议。
- 明确它如何支撑后续 `swatplus` 路线。
- 如有代码修改，保持小而真实，基于现有源码而不是另起体系。

