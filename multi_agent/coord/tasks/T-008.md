# TASK T-008

OWNER: w1

## Goal

- 评估当前 `version: swat` 模板与运行链路中哪些部分是强绑定 SWAT 2012 的。
- 输出一份“现有 SWAT 2012 适配点清单”，为后续接入 SWAT+ 提供边界图。

## Must Know Before Execution

- 这轮任务以调研和方案为主，不修改代码。
- 重点找“绑定在哪里”，不要泛泛描述架构。
- 以源码为准，优先看 `models/swat/`、`config/loader.py`、`validation/`、`examples/`、`tests/`。
- 结果要能回答：哪些模块可复用，哪些模块大概率要新建或重写。

## Scope

- 阅读并分析：
- `src/hydropilot/models/swat/`
- `src/hydropilot/config/loader.py`
- `src/hydropilot/validation/`
- 与 SWAT 相关的 `examples/` 和 `tests/`
- 输出聚焦于模板层、校验层、项目发现层、参数库/序列库依赖。

## Out Of Scope

- 不研究 SWAT+ 外部资料。
- 不修改任何源码或文档。
- 不提出过细的实现代码，只做结构化调研。

## Done When

- 给出一份清晰的 SWAT 2012 绑定点清单。
- 至少分成“可复用 / 需抽象 / 需新建”三类。
- 标出 3 到 5 个最关键的接入风险点。
