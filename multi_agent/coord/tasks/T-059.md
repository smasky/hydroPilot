# TASK T-059

OWNER: w1

## Goal

- Implement the first real swatplus filter expansion path for calibration.cal so filter is no longer only preserved metadata.

## Must Know Before Execution

- Current builder keeps filter on physical entries but does not expand it into final calibration targeting.
- Discovery already provides hru_meta with lu_mgt / soil / hydro_name.
- Keep swatplus and swat2012 separate.

## Scope

- Design and implement the minimal actual targeting strategy grounded in current code and known SWAT+ calibration structure.
- Prefer the most defensible MVP that turns filter into real output semantics without over-claiming unsupported native condition syntax.
- Update builder/template/tests as needed.
- If final output still uses a constrained subset of targeting semantics, make that explicit in tests and summary.

## Out Of Scope

- No broad redesign of general writer layer.
- No speculative support for every possible native condition form.

## Done When

- A swatplus filter changes generated calibration targeting in a real, testable way.
- Tests prove filter is no longer inert metadata.
- Result and remaining boundaries are clearly stated.
