# TASK T-059-R1 (Revision)

OWNER: w1

## Goal

- Fix object-space correctness: constrain filter→object-ID expansion to only HRU-scoped parameters. Reject or fail-fast on filter usage for basin/non-HRU params.

## Facts

- Current builder resolves ALL physical param filters through HRU metadata and writes HRU IDs into OBJ_TOT tail.
- Calibration object-id semantics depend on parameter object space (ob_typ), not a universal HRU space.
- Basin-level params (parameters.bsn) should NOT get HRU IDs in their tail.
- This is currently "HRU-scoped targeting MVP" — not "universal filter targeting."

## Scope

- Review and fix: `src/hydropilot/models/swatplus/builder.py`, `validate.py` if needed.
- Active filter→object-ID expansion ONLY for HRU-targeted params (hydrology.hyd, hru-level).
- For basin/non-HRU params with filter: either reject with clear error, or skip filter and mark OBJ_TOT=0.
- Update docs from T-061 so they do NOT imply all SWAT+ params with filter are supported.
- Add targeted tests.

## Out Of Scope

- No native CONDS.
- No new object types (channel, aquifer, reservoir).
- No range support.

## Done When

- Filter targeting scoped to HRU-level params only.
- Basin params with filter handled safely (rejected or skipped).
- Tests pass.
- Clear boundary documented.
