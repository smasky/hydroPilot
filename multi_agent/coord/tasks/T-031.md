# TASK T-031

OWNER: w2

## Goal

- 从 `swat2012` 参数库中筛出“不能直接搬，但值得参考后重建”的参数组。
- 给 leader 一份按文件/对象层级组织的重建候选清单。

## Must Know Before Execution

- 这是调研/映射任务，不是实现任务。
- 不要把 `swat2012` 的 `file/line/start/width` 直接视为可复用。
- 关注那些在 `swatplus` 里大概率仍有对应语义，但当前 `swatplus_db.yaml` 还没覆盖的参数。

## Scope

- 阅读：
- `src/hydropilot/models/swat/swat_db.yaml`
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 输出：
- 一个“需参考后重建”的参数组清单
- 按主题或对象层级分组，例如：
- gw / soil / channel / routing / nutrient / management
- 每组说明：
- 为什么不能直接搬
- 为什么仍值得进入下一轮扩参 backlog

## Out Of Scope

- 不修改源码
- 不修改文档
- 不给出未经源码支持的 SWAT+ 字段定位

## Done When

- 给出一份结构化的“重建候选池”
- 清楚区分“可参考语义”和“不可复用实现”
