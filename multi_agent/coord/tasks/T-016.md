# TASK T-016

OWNER: w3

## Goal

- 为 `swatplus` 第一阶段实现第一批验证。
- 验证必须贴合当前确定的 MVP 表面能力，不预设 filter、CSV 参数默认或 XAJ 语义。

## Must Know Before Execution

- 这轮是实现任务，不是纯规划。
- 只围绕当前已定边界写测试：
- `TxtInOut + txt/csv + 少量参数 + 少量输出 + 无 filter`
- 测试要优先锁模板展开、配置链路、reader/writer 调度。
- 你不是一个人在代码库里工作，不要回退别人做的改动；若接口有小变化，按最新实现适配。

## Scope

- 新增或修改测试文件，例如：
- `tests/test_swatplus_template.py`
- 如有必要，补少量通用测试或动态 fixture helper
- 实现第一批测试：
- 模板展开为 `general`
- `prepare_config()` / `load_config()` 对 `swatplus` 生效
- 参数库自动补全
- series 简写展开
- reader/writer 注册与调度
- 非法工程目录 / 非法参数名 / 非法变量名的校验

## Out Of Scope

- 不做 filter 测试
- 不做 db/netcdf 测试
- 不依赖外部 SWAT+ 工程
- 不做文档改动

## Done When

- 第一批 `swatplus` 测试落地
- 测试夹具自包含，可在 `tmp_path` 下生成最小工程
- 汇报已通过的测试、未通过项和阻塞点
