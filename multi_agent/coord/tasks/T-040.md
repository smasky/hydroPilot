# TASK T-040

OWNER: w2

## Goal

- 收敛 `hyd-sed-lte.cha` 剩余可入库参数清单，只保留已满足“源码支持 + 真实落点确认”的候选。

## Must Know Before Execution

- owner 已明确：`cha` 组只要证据闭环，就不再因为“优先级低”继续挂着。
- 当前先做证据收敛，不直接改 `swatplus_db.yaml`。
- 需要尽量基于真实 `Osu_1hru` 文件，而不是推测。

## Scope

- 基于 `Osu_1hru` 的 `hyd-sed-lte.cha`：
  - 核出当前已入库 6 个之外，仍可继续入库的参数
  - 确认真实列位置
  - 区分：
    - 立即可入库
    - 结构性/不建议入库
- 对已知候选如 `wd / dp / slp / len / wd_rto / eq_slp / side_slp / bed_load / fps / fpn / n_conc / p_conc` 给出逐项判断。

## Out Of Scope

- 不直接改 `swatplus_db.yaml`
- 不处理 `bsn / soils / aquifer / reservoir`
- 不改 builder / writer

## Done When

- 给出一份 `cha` 剩余候选清单
- 每个候选都附带“可入库 / 不建议入库 / 证据不足”判断
- 对可入库项给出真实文件落点信息

