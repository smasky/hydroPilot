# TASK T-048

OWNER: w1

## Goal

- 直接实施 `swatplus` series 库第一轮补全，优先补流量相关的 `yr / mo / da` 条目，并修正现有明显错误的流量列位置信息。

## Must Know Before Execution

- owner 已确认：现在可以开始 `swatplus series` 层级补全。
- 已有 accepted 结论：
  - SWAT2012 `output.rch` 最接近 `SWAT+ channel_* + flo_out`
  - `hydout_*` 是监测点路线
  - `series.py` 已支持 `aa / yr / mo / da`，本轮以 DB 扩展为主，不先改代码
- T-046 已确认：
  - Tier 1 最优先：`hydout_*`, `hydin_*`, `basin_wb_*`, `channel_*`
  - 需要注意现有 `hydout_aa/hydin_aa` 的列位置信息可能错误

## Scope

- 直接修改：
  - `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 最小实施目标：
  - 补 Tier 1 的 flow-priority 系列条目，至少覆盖 `yr`
  - 在证据充分时继续补 `mo / da`
  - 修正现有 `hydout_aa` / `hydin_aa` 的列位置信息（如果调研已足够支持）
  - 保持与现有 `series.py` / `library.py` 兼容
- 修改后需给出：
  - 新增 series 条目数
  - 各频率新增数
  - 是否修正了现有流量条目

## Out Of Scope

- 不改 `series.py` 代码，除非发现 DB 方案无法落地且确有必要
- 不大规模补非流量优先的系列
- 不处理参数库 (`parameters:`)

## Done When

- `swatplus_db.yaml` 完成第一轮 series 补全
- 流量相关 `yr / mo / da` 条目进入库中
- 现有明显错误的流量列位信息得到修正或明确说明

