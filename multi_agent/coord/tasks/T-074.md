# TASK T-074

OWNER: w3

## Goal

- Confirm from SWAT+ source code + real project files what object IDs calibration.cal for ob_typ=sol should actually carry in practice.

## Must Know

- FORTRAN source (`cal_parmchg_read.f90`) shows: for `ob_typ=sol` with `OBJ_TOT=0`, `num_elem = sp_ob%hru` and `num = [1..sp_ob%hru]`.
- This means the object space for sol IS HRU-indexed, not soil-profile-indexed.
- Need to verify end-to-end: what IDs does a real calibration.cal actually carry for soil params?

## Scope

- Read SWAT+ FORTRAN source thoroughly: cal_parmchg_read.f90, calibration_data_module.f90, soil_module.f90.
- Confirm: does `ob_typ=sol` use HRU IDs or soil profile IDs in the object tail?
- Check: are there any calibration examples or documentation showing sol targeting?
- Output: definitive statement on what IDs belong in sol calibration records.

## Out Of Scope

- No code changes.
- No filter implementation decisions.

## Done When

- Clear statement: sol calibration objects = HRU IDs (confirmed) OR soil profile IDs (confirmed).
- Source citations provided.
