# TASK T-039

OWNER: w1

## Goal

- 收敛 `parameters.bsn` 剩余可入库参数清单，并核准当前 `swatplus_db.yaml` / T-038 的数量口径。

## Must Know Before Execution

- owner 已明确：`bsn / sol / cha` 里，只要**源码支持且真实文件落点已确认**，就应入库。
- 当前不希望多人同时直接改 `swatplus_db.yaml`，这一轮先收证据，不做大规模实现。
- T-038 的汇报里存在数量口径不一致，需一并核准。

## Scope

- 基于真实 `Ames_sub1` 文件与当前 `swatplus_db.yaml`：
  - 核准当前参数总数、各组小计、T-038 实际新增数
  - 整理 `parameters.bsn` 里仍未入库但已具备真实落点证据的参数
  - 区分：
    - 立即可入库
    - 虽有落点但仍建议不入库
- 产出必须可直接用于下一轮实施。

## Out Of Scope

- 不直接改 `swatplus_db.yaml`
- 不处理 `sol / cha / aquifer / reservoir`
- 不改 builder / writer / runtime

## Done When

- 给出一份核准后的 `bsn` 剩余候选清单
- 给出 T-038 数量口径修正结果
- 明确哪些 `bsn` 参数下一轮应直接入库

