# TASK T-011

OWNER: w1

## Goal

- 基于当前 `swat` 模板源码，产出一份 `swatplus` 的实现方案草案。
- 重点给出模块职责、阶段边界、以及第一阶段必须支持的最小能力集合。

## Must Know Before Execution

- 这轮任务以方案为主，不修改代码。
- 以当前仓库源码结构为准，不引入 `calibration.cal`，不改变 `general` 核心定位。
- `swatplus` 按新模板处理，目录目标是 `src/hydropilot/models/swatplus/`。
- 第一阶段边界已经确定为：`TxtInOut + txt/csv 输出 + 少量参数 + 少量输出对象 + 带验证`。

## Scope

- 阅读并分析：
- `src/hydropilot/models/swat/`
- `src/hydropilot/config/loader.py`
- `src/hydropilot/models/registry.py`
- 结合 leader 已确定的方向，输出：
- `swatplus` 模块清单
- 每个模块的职责
- 第一阶段支持范围
- 后续阶段扩展范围

## Out Of Scope

- 不修改源码。
- 不写测试代码。
- 不研究 `db/netcdf` 详细实现。

## Done When

- 给出一份结构化实现方案。
- 至少包含：
- 模块分层
- MVP 边界
- 需要小改的现有模块
- 后续扩展点
- 明确指出 loader/template 接口层最需要先抽象的点。
