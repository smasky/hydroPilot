# TASK T-032

OWNER: w3

## Goal

- 从 `swat2012` 参数库中筛出当前不建议动的参数，避免下一轮扩参误入高风险区域。
- 产出一份“暂缓项/高风险项”清单。

## Must Know Before Execution

- 这是调研/映射任务，不是实现任务。
- 当前 `swatplus` 仍然是 Ames_sub1 驱动的阶段，不要把缺少样本支撑的参数硬判为可迁移。
- 特别关注：
- 严重依赖对象层级的参数
- 严重依赖更成熟 filter 语义的参数
- 只在 `swat2012` 可见、在当前 `swatplus` 中缺少事实锚点的参数

## Scope

- 阅读：
- `src/hydropilot/models/swat/swat_db.yaml`
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 输出：
- “暂不建议迁移”的参数类别或代表参数
- 每类写清风险原因
- 尽量给出 leader 可直接引用的判断语句

## Out Of Scope

- 不修改源码
- 不修改文档
- 不泛泛而谈“以后再说”，要说清具体风险

## Done When

- 给出一份高风险/暂缓清单
- 风险理由足够具体，能指导后续 backlog 排序
