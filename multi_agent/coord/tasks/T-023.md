# TASK T-023

OWNER: w1

## Goal

- 继续基于真实 `Ames_sub1` 样本，补齐 `swatplus` 当前剩余的 discovery / validation MVP 缺口。
- 在不扩张范围的前提下，把第一阶段所需的最小真实能力继续做实。

## Must Know Before Execution

- 这轮是实现任务。
- 样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `models/swat` 与 `models/swatplus` 必须继续保持独立。
- 只抽真正模型无关的 helper，不合并两套模型知识。
- 第一阶段仍然无 `filter`，不被本机 executable E2E 阻塞。

## Scope

- 继续完善：
- `src/hydropilot/models/swatplus/discovery.py`
- `src/hydropilot/models/swatplus/validate.py`
- 必要时少量联动 `template.py`
- 重点：
- 把当前最小真实解析做得更稳
- 收紧 validator，使错误信息贴近真实 SWAT+ 文件结构
- 消除明显冗余和占位式兼容写法

## Out Of Scope

- 不做 filter
- 不改 `models/swat/`
- 不做文档改动
- 不扩展到完整 SWAT+ 对象体系

## Done When

- `discovery/validate` 比当前更贴近真实样本
- 冗余或临时占位逻辑减少
- 清楚说明仍未覆盖的 SWAT+ 结构
