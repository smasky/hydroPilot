# TASK T-050

OWNER: w3

## Goal

- 基于 T-048 的补库结果，整理一份面向用户的 `swatplus flow series` 使用说明最小稿。

## Must Know Before Execution

- owner 当前明确关心：
  - SWAT2012 `output.rch` 在 SWAT+ 里怎么取
  - series 库补齐后用户到底该怎么写
- 这次不直接改主文档，但要给 leader 一份可直接转 md/docs 的草稿。

## Scope

- 基于最终补库结果，整理：
  - `channel_* + flo_out`
  - `hydout_* + flo`
  - `basin_wb_* + WYLD`
  这三条主路线的最小使用说明
- 尽量写成接近最终文档的格式，方便 leader 后续直接采用

## Out Of Scope

- 不改 `swatplus_db.yaml`
- 不改 `series.py`
- 不做实现 audit

## Done When

- 给出一份可直接转正式文档的最小稿
- 覆盖最常用的流量抽取场景

