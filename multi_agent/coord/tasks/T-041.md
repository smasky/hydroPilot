# TASK T-041

OWNER: w3

## Goal

- 收敛 `soils.sol` 剩余可入库参数清单，排除 owner 已明确不建议继续入库的土壤质地/低优先级项。

## Must Know Before Execution

- owner 已明确：`sol` 组里，只要**源码支持且真实文件落点已确认**，就应入库。
- 但当前明确不建议继续推进的项包括：
  - `clay / silt / sand / rock`
  - `ec / cal / ph`
- 当前先做证据收敛，不直接改 `swatplus_db.yaml`。

## Scope

- 基于真实 `Ames_sub1` `soils.sol` 文件：
  - 核出当前已入库 8 个之外，仍可继续入库的参数
  - 区分 profile-level 与 layer-level
  - 只保留 owner 当前口径下仍适合入库的项
- 同时说明这些候选在后续 builder 接线时是否会遇到多层展开约束。

## Out Of Scope

- 不直接改 `swatplus_db.yaml`
- 不把 `clay / silt / sand / rock / ec / cal / ph` 重新拉回候选
- 不处理 `bsn / cha / aquifer / reservoir`

## Done When

- 给出一份 `soils.sol` 剩余候选清单
- 明确哪些可以下一轮直接入库
- 对每个可入库候选给出真实文件落点信息

