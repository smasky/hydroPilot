# TASK T-075

OWNER: w1

## Goal

- Reopen soils.sol filter support using confirmed HRU-ID semantics from T-074.

## Source Confirmation

- `cal_parmchg_read.f90`: `case("sol")` → `num_elem = sp_ob%hru` — sol uses HRU IDs
- `soil_data_module.f90`: `Index:(layer,HRU)` — soil properties indexed by HRU
- **soil filter → HRU ID path is semantically CORRECT**

## Scope

- Restore `soils.sol` to `_FILTERABLE_SOURCE_FILES` and `_HRU_SCOPED_ORIGIN_FILES`.
- Soil-name filter resolves to HRU IDs via hru_meta.soil (exact match).
- Hydro-suffix limitation documented (soil_01 vs soil_01-h1 — exact match only).
- Update tests: soil filter accepted, resolves to correct HRU IDs in calibration.cal tail.
- Update docs if T-071 needs correction (sol status from deferred → implemented).
- Keep hru/bsn/cha unchanged.

## Out Of Scope

- No HYD_GRP/TEXTURE/layer selectors.
- No hydro-suffix expansion (exact match only).

## Done When

- Soil filter on sol params: accepted at validation, resolves to HRU IDs.
- Tests pass.
- Final status: "sol targeting is HRU-indexed per SWAT+ FORTRAN."
