# TASK T-044

OWNER: w3

## Goal

- 评估 T-042 改库后对后续实现的直接影响，重点是 `builder` / 多层展开 / 现有说明文档的同步风险。

## Must Know Before Execution

- 这次不直接改 `swatplus_db.yaml`。
- 你的任务不是重做参数证据，而是站在“改完库以后，后面实现会卡在哪里”的角度补风险清单。
- owner 目前先要把库收进去，但后续接线风险也要看清楚。

## Scope

- 基于 T-042 最终改库结果，评估：
  - `soils.sol` 新增参数对多层展开的直接约束
  - `hyd-sed-lte.cha` 新增参数对 builder/filter 的约束
  - 当前注释/文档是否需要补一两处高信号说明
- 如果存在“现在入库没问题，但实现前必须提醒”的点，整理成简明清单。

## Out Of Scope

- 不改 `swatplus_db.yaml`
- 不实现 builder / runtime 代码
- 不扩展 `aquifer / reservoir`

## Done When

- 给出一份 T-042 之后的后续实现风险与注意事项清单
- 帮助 leader 判断这轮是否可以把库先收完、实现下轮再接

