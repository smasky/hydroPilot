# TASK T-064

OWNER: w3

## Goal

- Real-project verification: soils.sol soil param with filter is rejected at validation. Also confirm no hidden builder path applies it when validation is bypassed.

## Must Know

- Soil param = source file soils.sol (e.g., sol_bd).
- Expected error: same "filter is only supported for per-HRU parameters" rule.
- If practical, also verify builder scope gate catches it if validation bypassed.

## Scope

- Write a minimal swatplus config with one soil param + filter.
- Run validation → confirm rejection.
- Optionally: bypass validation, run build, confirm OBJ_TOT=0 (builder defense-in-depth).

## Done When

- Result: "rejected-at-validation" with config + error. Optional: "builder-gate-confirmed" with OBJ_TOT=0.
