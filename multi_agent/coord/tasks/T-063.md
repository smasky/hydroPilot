# TASK T-063

OWNER: w2

## Goal

- Real-project verification: parameters.bsn basin param with filter is rejected at validation with expected fail-fast message.

## Must Know

- Basin param = source file parameters.bsn (e.g., surq_lag).
- Expected error: "filter is only supported for per-HRU parameters".

## Scope

- Write a minimal swatplus config with one basin param + filter.
- Run through validation only (no need to build).
- Confirm: validation error with expected message.

## Done When

- Result: "rejected-at-validation" with exact config and error message.
