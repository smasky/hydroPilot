# TASK T-066

OWNER: w1

## Goal

- 明确 bsn 参数在 calibration.cal 中的最小有意义的 targeting 语义。提出一个不与 SWAT+ 强绑定的通用层 targeting 抽象。

## Must Know

- bsn 参数（parameters.bsn）是 basin 级全局参数——全流域只有一个值。
- 当前 filter→object-ID 展开仅支持 hydrology.hyd（HRU-scoped），bsn 参数上的 filter 被拒绝。
- 需要回答：bsn filter 是否应该 resolve 到单一全局 basin object？还是无 selector？还是某种受限 selector？

## Scope

1. **bsn targeting 语义**：
   - 在 SWAT+ calibration.cal 中，basin 级参数如何表达 targeting？
   - 是否需要 OBJ_TOT？如果需要，object ID 是什么（固定为 1？）
   - 参考 FORTRAN 源码和 calibration 文档
2. **通用层 targeting 抽象**：
   - 提出一个不与 SWAT+ 强标记的通用抽象
   - 可携带：object space + selector + resolved IDs
   - 基于当前 `_resolved_ids` 模式但不硬编码 HRU
3. **产出**：bsn 的推荐 MVP selector + 通用抽象草案

## Out Of Scope

- 不实现代码。
- 不扩展到 aquifer/reservoir。

## Done When

- bsn 最小 targeting 语义明确
- 通用抽象草案完成
