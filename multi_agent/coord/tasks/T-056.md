# TASK T-056

OWNER: w1

## Goal

- Fix apply_design/apply_physical so copied project outputs also run the same initialization boundary needed for swatplus calibration.cal skeleton deployment.

## Must Know Before Execution

- Current Session path initializes instances via paramWritePlan.initialize(instance_path).
- Current apply.py copies the project and directly calls paramApplier.apply(), which leaves swatplus calibration.cal empty in the output directory.
- We already verified this on a real Ames_sub1 smoke run.

## Scope

- Minimal fix in apply path only.
- Reuse existing initializer contract; do not create a second special swatplus apply path.
- Add targeted tests locking that apply output contains initialized calibration.cal when needed.

## Out Of Scope

- No broader refactor.
- No changes to unrelated run/session flow.

## Done When

- apply_design/apply_physical produce initialized calibration.cal for swatplus outputs.
- targeted tests pass.
- result summarized clearly.
