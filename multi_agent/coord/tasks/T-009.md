# TASK T-009

OWNER: w2

## Goal

- 从参数写入、序列提取、输出读取三个角度，评估当前框架对 SWAT+ 的 IO 适配难点。
- 重点看当前 `fixed_width/text` 假设在哪些地方会成为 SWAT+ 接入阻力。

## Must Know Before Execution

- 这轮任务以调研和方案为主，不修改代码。
- 不需要查外部 SWAT+ 资料；只基于当前仓库能力判断“若接入一个不同文件结构的模型，需要动哪里”。
- 输出要具体到模块和职责，不要只说“可能需要新模板”。

## Scope

- 阅读并分析：
- `src/hydropilot/io/`
- `src/hydropilot/params/`
- `src/hydropilot/series/`
- `src/hydropilot/runtime/services.py`
- 与 CSV/fixed_width/text 相关的实现和测试
- 重点回答：
- 哪些 reader/writer/runner 可直接复用
- 哪些假设会卡住 SWAT+
- 如果 SWAT+ 采用不同输入输出组织，最小改动路径是什么

## Out Of Scope

- 不调研 SWAT+ 外部文件格式细节。
- 不修改代码。
- 不写实现方案的伪代码。

## Done When

- 给出一份 IO 适配评估。
- 至少包含“直接复用能力”“需要扩展能力”“潜在阻塞点”三部分。
- 标出最值得优先验证的 2 到 4 个技术假设。
