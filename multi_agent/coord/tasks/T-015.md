# TASK T-015

OWNER: w2

## Goal

- 在当前框架下落出 `swatplus` 的最小模板骨架。
- 第一阶段只做窄范围 MVP：`TxtInOut + txt/csv + 少量参数 + 少量输出 + 无 filter`。

## Must Know Before Execution

- 这轮是实现任务，不是调研。
- 必须遵循 hydroPilot 现有模板骨架：`template/discovery/library/builder/series/validate`。
- 第一阶段不做 `filter`，不做 `calibration.cal`，不做 `db/netcdf`。
- 你不是一个人在代码库里工作，不要回退别人做的改动；如果遇到其他人修改，按当前最新状态适配。

## Scope

- 新增或修改：
- `src/hydropilot/models/swatplus/`
- `src/hydropilot/models/registry.py`
- 必要时补最小示例或测试夹具代码，但不要和验证任务抢同一测试文件的大块写入
- 实现最小能力：
- `version: swatplus` 注册
- 最小 `SwatPlusTemplate`
- discovery 最小只拿时间字段 + `n_spatial_units`
- library/db 支撑少量参数与少量输出变量
- builder 支持无 filter 的参数展开
- series 支持简写到 general reader spec 的展开
- validate 支持最小模板校验

## Out Of Scope

- 不做 filter
- 不做 transformer 扩展
- 不做完整参数库
- 不做文档改动

## Done When

- `version: swatplus` 能进入模板展开链路
- 能生成合法的 `general` 原始配置
- 第一阶段边界保持收敛
- 列出改动文件、已实现能力、未覆盖边界
