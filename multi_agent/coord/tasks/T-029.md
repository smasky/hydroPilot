# TASK T-029

OWNER: w3

## Goal

- 修正 `swatplus` 第一版 `filter` 的实现偏差，让它真正满足 leader review 结论。
- 只做最小 revision，不扩新能力，不重做参数/输出部分。

## Must Know Before Execution

- 这轮是 revision 任务。
- 真实样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `models/swatplus/` 与 `models/swat/` 必须继续独立
- 用户要求：公共写法尽量贴近 `swat2012`，但不要伪装成已经支持了 SWAT 2012 那套完整 filter 维度
- 以当前源码和真实样本为事实，不要沿用 coordinator 上一版错误汇报

## Leader Findings To Fix

- 当前实际支持的 filter 字段与汇报不一致：代码只支持 `lu_mgt / soil / hydro_name`
- `validate` 与 `build` 语义不一致：validate 对未知字段仅 warning，但 build 会直接报错
- `filter` 当前未限制适用文件，误用于非 HRU 文件时可能生成错误 `lineList`
- filter 字段常量分散在多个文件，已经产生冗余与漂移风险

## Scope

- 修改：
- `src/hydropilot/models/swatplus/discovery.py`
- `src/hydropilot/models/swatplus/builder.py`
- `src/hydropilot/models/swatplus/validate.py`
- `tests/test_swatplus_template.py`
- 如有必要，可少量联动 `template.py` 或 `library.py`，但不要碰 `models/swat/`
- 目标：
- 明确 SWAT+ Phase 1 filter 当前真实支持边界
- 统一 validate/build 的语义
- 限制 filter 仅用于当前真实可支撑的参数文件类型
- 消除 filter 字段定义的重复来源

## Preferred Direction

- 如果当前真实样本只支撑 `lu_mgt / soil / hydro_name`，那就明确写死为“当前仅支持这三项”，不要冒充 `subbasin / land_use / slope` 已支持
- `validate` 与 `build` 的行为要一致，优先尽早报清晰错误
- 对非 HRU 目标文件，如果 filter 不适用，应明确拒绝，而不是生成伪造的 `lineList`
- 把 filter 支持字段收敛到单一事实来源，其他模块引用它

## Out Of Scope

- 不新增更多 filter 维度
- 不改参数库扩展结果
- 不改输出/series 扩展结果
- 不做文档改动
- 不依赖 executable E2E

## Done When

- leader review 提出的 3 个问题 + 1 个冗余点都被修掉
- 测试覆盖新的边界与失败场景
- 最终对外语义清楚：当前 SWAT+ filter 到底支持什么、不支持什么
