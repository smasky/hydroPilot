# TASK T-028

OWNER: w3

## Goal

- 启动 `swatplus` Phase 2 的最小 `filter` 设计与实现收口。
- 在保持模板风格贴近 `swat2012` 的同时，只引入当前真实样本能支撑的最小过滤能力。

## Must Know Before Execution

- 这轮是实现任务。
- 样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `filter` 是新增能力，但不要扩张到完整对象体系
- 写法尽量靠近 `swat2012` 的 `parameters.physical[].filter`
- 不要把未验证的 SWAT+ 空间层级假设硬编码进模板

## Scope

- 修改：
- `src/hydropilot/models/swatplus/discovery.py`
- `src/hydropilot/models/swatplus/builder.py`
- `src/hydropilot/models/swatplus/validate.py`
- 如有必要补少量测试到现有 swatplus 测试文件
- 目标：
- 基于真实样本提取最小可支持的过滤元数据
- 支持一小组明确、可解释的 filter 字段
- 保持与 swat2012 相近的用户写法

## Out Of Scope

- 不做完整 SWAT+ 对象层级支持
- 不改 `models/swat/`
- 不做文档改动
- 不依赖 executable E2E

## Done When

- `swatplus` 有第一批真实可用的 filter 能力
- 模板写法与 `swat2012` 风格保持接近
- 明确 filter 当前支持边界与限制
