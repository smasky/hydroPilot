# TASK T-062

OWNER: w1

## Goal

- Real-project verification: hydrology.hyd HRU-scoped param with filter passes validation, expands to calibration.cal with nonzero OBJ_TOT/object tail.

## Must Know

- Use Ames_sub1 or another runnable sample project already available.
- HRU-scoped param = source file hydrology.hyd (e.g., esco).
- Filter must resolve through actual project hru_meta.

## Scope

- Write a minimal swatplus config with one HRU param + filter.
- Run through prepare_config → validation → build_config → verify skeleton output.
- Confirm: validation passes, OBJ_TOT > 0, object IDs appear in tail.

## Done When

- Result: "accepted+active" with exact config, observed output, and OBJ_TOT count.
