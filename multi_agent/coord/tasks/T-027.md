# TASK T-027

OWNER: w2

## Goal

- 扩展 `swatplus` 的输出覆盖与变量映射。
- 在保持模板风格贴近 `swat2012` 的前提下，让更多真实 SWAT+ 输出可被 series 使用。

## Must Know Before Execution

- 这轮是实现任务。
- 样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `models/swatplus/` 与 `models/swat/` 逻辑独立
- 公共写法尽量贴 `swat2012` 的 `series.sim.file/id/period/variable` 心智
- 第一阶段仍不被本机 executable E2E 阻塞

## Scope

- 修改：
- `src/hydropilot/models/swatplus/series.py`
- `src/hydropilot/models/swatplus/library.py`
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 必要时联动 `validate.py`
- 目标：
- 增加真实输出文件/变量映射
- 继续压实最小可用 row selection 行为
- 清理冗余或错误的占位输出定义

## Out Of Scope

- 不做 filter
- 不改 `models/swat/`
- 不做完整全量 SWAT+ 输出体系
- 不做文档改动

## Done When

- 输出覆盖较当前明显提升
- `series` 写法仍尽量保持贴近 `swat2012`
- 已支持/未支持输出能力边界清楚
