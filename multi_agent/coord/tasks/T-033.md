# TASK T-033

OWNER: w1

## Goal

- 为 hydroPilot 增加公用的 `instance initialize` 阶段设计与最小实现方案。
- 目标是放入现有 runtime 骨架，不给 `swatplus` 开特权通道。

## Must Know Before Execution

- 当前 owner 已明确：初始化阶段应该在项目副本创建后执行一次。
- 初始化阶段应写完 `formatted_text` 文件的静态结构，例如 header / 记录骨架。
- 运行阶段只负责动态值写入。
- 流程骨架必须保持统一，不能把 `swatplus` 做成运行前特殊补丁。

## Scope

- 基于真实源码评估并实现最小 runtime 挂点。
- 优先考虑：
  - `runtime/session.py`
  - `runtime/services.py`
  - `runtime/workspace.py`
  - `io/writers/base.py`
- 给出或实现一个公用 `instance initializer` 机制，要求：
  - 不破坏现有 `Workspace` 职责边界
  - 不把逻辑塞回 `Executor.run()`
  - 支持 writer 声明“我需要初始化”
- 如果做代码修改，保持最小、可测试、可回退。

## Out Of Scope

- 不实现 `swatplus` 业务细节。
- 不在这个任务里实现完整 `formatted_text` writer。
- 不修改 `swatplus` builder 的参数翻译逻辑。

## Done When

- 明确推荐的 runtime 挂载位置与理由。
- 如果已实现，代码中存在一个公用初始化入口，且不带模型专属命名。
- 给出最小验证方式，说明初始化在 session 级只执行一次。

