# TASK T-069

OWNER: w1

## Goal

- bsn formalization + sol soil-name targeting implementation.

## Must Know

- bsn: OBJ_TOT=0 already correct. No runtime change needed. Formalize in docs/validate.
- sol: soil-name-based targeting through existing soil/HRU path. soil field already in FILTERABLE_HRU_FIELDS and hru_meta.
- Do NOT add HYD_GRP, TEXTURE, or layer selectors in this round.

## Scope

**bsn:**
- No code change. Update validate.py to explicitly allow bsn params without filter (current behavior).
- Document: bsn = global basin targeting, OBJ_TOT=0, no filter support.

**sol:**
- Extend `_FILTERABLE_SOURCE_FILES` or equivalent to allow soils.sol params with soil-name filter.
- Implement soil-name filter resolution through existing hru_meta soil field.
- Handle hydro-suffix convention (e.g., soil_01 vs soil_01-h1) — recommend exact match MVP, document the limitation.
- Add validation tests for soil filter acceptance/rejection.
- Add targeting tests proving soil filter resolves to correct HRU IDs.

## Out Of Scope

- No HYD_GRP/TEXTURE/layer selectors.
- No soils.sol parsing in discovery.py.
- No cha implementation.

## Done When

- bsn status formalized.
- sol soil-name filter works end-to-end.
- Tests pass.
