# TASK T-070

OWNER: w2

## Goal

- Implement cha object_id passthrough targeting with discovery and validation.

## Must Know

- Channel IDs = sequential row numbers in channel-lte.cha (1..n_reaches).
- object_id filter key already supported by T-059 mechanism.
- object.cnt cha column does NOT count channel reaches.
- Do NOT add name/order selectors in this round.

## Scope

- Add channel-lte.cha parsing to discovery.py (~30 lines, similar to hru-data.hru pattern).
- Extract: n_reaches, reach_ids list.
- Extend builder validation/filter gate to allow hyd-sed-lte.cha params with object_id filter.
- Add validation: reject non-object_id filter keys for cha, validate reach ID range [1..n_reaches].
- Add tests: cha object_id filter → OBJ_TOT > 0 with correct IDs; non-object_id filter → rejected; out-of-range ID → rejected.

## Out Of Scope

- No name/order selectors.
- No channel attribute-based filter (no lu_mgt equivalent for channels).

## Done When

- cha object_id targeting works end-to-end.
- discovery provides reach metadata.
- Tests pass.
