# TASK T-026

OWNER: w1

## Goal

- 扩展 `swatplus` 的参数覆盖，但保持 Phase 1/1.5 范围内的收敛。
- 在不引入模型逻辑混合的前提下，让参数库和参数写入路径更贴近真实 SWAT+ 样本。

## Must Know Before Execution

- 这轮是实现任务。
- 真实样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `models/swatplus/` 必须独立于 `models/swat/`
- 公共写法尽量贴合 `swat2012`，但不要把 SWAT 2012 的模型知识搬进来
- 先扩参数覆盖，不做文档改动

## Scope

- 修改：
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 必要时联动 `library.py` / `builder.py` / `validate.py`
- 目标：
- 扩大当前可用参数集合
- 校验参数定义是否与真实样本文件结构一致
- 清理低价值占位参数定义与冗余映射

## Out Of Scope

- 不做 filter
- 不做 executable E2E
- 不改 `models/swat/`
- 不做文档改动

## Done When

- 参数覆盖较当前明显增加
- 参数库更贴近真实样本
- 已支持/未支持参数边界清楚
