# TASK T-021

OWNER: w2

## Goal

- 基于真实样本 `Ames_sub1`，收口 `swatplus` 的 series 逻辑。
- 把当前只做 `variable -> colSpan` 的占位实现，推进到更接近真实 general series 规格。

## Must Know Before Execution

- 这轮是实现任务。
- 样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- 第一阶段仍保持无 filter。
- `models/swatplus/` 独立于 `models/swat/`，但可复用通用 helper。
- 消除冗余，不要复制 SWAT 2012 整套实现。

## Scope

- 修改：
- `src/hydropilot/models/swatplus/series.py`
- 如有必要，可补 `library.py` / `validate.py`
- 目标：
- 校正真实样本下的输出文件/变量映射
- 评估并尽量实现 `id + period` 到 general 行选择的真实展开
- 若暂时做不到完整自动展开，也要把失败模式和显式要求收紧到真实口径

## Out Of Scope

- 不做 filter
- 不改 `models/swat/`
- 不做完整输出体系覆盖
- 不做文档改动

## Done When

- `series.py` 不再只是测试占位逻辑
- 对真实样本的最小输出路径有可解释实现
- 明确已支持与未支持的 `series` 能力边界
