# TASK T-042

OWNER: w1

## Goal

- 统一实施本轮 `swatplus_db.yaml` 校准与扩库：修正现有错位条目，并补入 `bsn / cha / sol` 已确认剩余参数。

## Must Know Before Execution

- owner 最新口径：只要**源码支持 + 真实文件落点已确认 + 确实可调**，就入库。
- 不再因为“优先级低 / 不常用 / 场景窄”挡住 `bsn / sol / cha` 参数。
- 当前已确认：
  - `bsn` 有 6 个现有错位条目必须修正
  - `bsn` 还有 9 个 ready 参数可补入
  - `cha` 还有 4 个 ready 参数可补入
  - `sol` 还有 2 个候选参数可补入
- 这次任务是本轮唯一直接修改 `swatplus_db.yaml` 的主实施任务。

## Scope

- 直接修改：
  - `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 实施内容包括：
  - 修正 6 个 `parameters.bsn` 错位条目
  - 补入 9 个 `bsn` ready 参数
  - 补入 4 个 `hyd-sed-lte.cha` ready 参数
  - 补入 2 个 `soils.sol` 候选参数
  - 视证据情况，同步修正已发现的 `cha` / `sol` 现有位置偏差
- 修改后必须做最基本的结构校验，并更新文件头/注释里与当前状态直接冲突的描述。

## Out Of Scope

- 不改 builder / writer / runtime 代码
- 不处理 `aquifer / reservoir`
- 不发明新的 DB schema

## Done When

- `swatplus_db.yaml` 完成本轮统一校准与扩库
- 变更项清单清楚、数量口径一致
- 给出修改后参数总数与各组小计

