# TASK T-024

OWNER: w2

## Goal

- 继续补 `swatplus` 的 series / library MVP 缺口。
- 在真实 `Ames_sub1` 结构下，让输出变量映射与最小行选择逻辑更可用、更少冗余。

## Must Know Before Execution

- 这轮是实现任务。
- 样本目录：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- `models/swatplus/` 独立于 `models/swat/`，但可以复用通用 helper。
- 第一阶段仍然无 `filter`。
- 不要为了图快把 SWAT 2012 的输出假设重新带回 `swatplus`。

## Scope

- 继续完善：
- `src/hydropilot/models/swatplus/series.py`
- `src/hydropilot/models/swatplus/library.py`
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 重点：
- 让真实样本中的输出文件与变量映射更完整
- 尽量进一步压实 row selection / series expansion 的真实行为
- 清理重复或低价值的占位映射

## Out Of Scope

- 不做 filter
- 不改 `models/swat/`
- 不做完整全量变量库
- 不做文档改动

## Done When

- `series/library/db` 比当前更贴近真实样本
- 冗余占位映射减少
- 已支持与未支持的输出能力边界更清楚
