# TASK T-020

OWNER: w1

## Goal

- 基于真实 SWAT+ 样本 `Ames_sub1`，收口 `swatplus` 的 discovery 逻辑。
- 去掉当前 discovery 中的硬编码返回，改为最小真实解析。

## Must Know Before Execution

- 这轮是实现任务。
- 真实样本目录为：`/Users/smasky/Downloads/swatplus-main/refdata/Ames_sub1`
- 只做最小必需解析，不扩展到完整 SWAT+ 语义。
- `models/swat` 与 `models/swatplus` 逻辑必须保持独立，不合并模型知识。
- 消除冗余，但不要把两套模型揉成一套。

## Scope

- 修改：
- `src/hydropilot/models/swatplus/discovery.py`
- 必要时少量联动 `validate.py` / `template.py`
- 目标：
- 从真实样本中解析最小时间字段
- 明确 `n_spatial_units` 的最小来源或临时策略
- 删除当前硬编码的 year/timestep/subbasin 返回
- 让 `discovery` 输出尽量贴近真实文件，而不是测试占位

## Out Of Scope

- 不做完整对象层级解析
- 不做 filter
- 不改 `models/swat/`
- 不做文档改动

## Done When

- `discovery.py` 不再硬编码 `2019/2021/daily/3`
- 能对 `Ames_sub1` 返回最小真实 meta
- 列出当前仍未解析的 SWAT+ 字段与原因
