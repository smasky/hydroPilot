# TASK T-051

OWNER: w1

## Goal

- Fix `src/hydropilot/models/swatplus/series.py` `_detectFrequency()` so it recognizes both real SWAT+ suffixes `_mon.txt`/`_day.txt` and legacy `_mo.txt`/`_da.txt`.

## Must Know Before Execution

- T-049 already confirmed DB naming is correct: SWAT+ source uses `_mon`/`_day`.
- This is a low-risk compatibility fix, not a series DB expansion.

## Scope

- Minimal code change in `series.py`.
- Add or update targeted tests covering both naming patterns.
- Keep behavior unchanged for `_aa`/`_yr`.

## Out Of Scope

- No further DB expansion.
- No broader refactor.

## Done When

- Frequency detection supports `aa`/`yr`/`mon`/`day` and `mo`/`da` aliases.
- Tests pass and result is summarized in c1.md.
