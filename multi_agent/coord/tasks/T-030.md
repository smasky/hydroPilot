# TASK T-030

OWNER: w1

## Goal

- 从 `models/swat/swat_db.yaml` 中筛出一批最有希望迁移到 `swatplus` 的“同义近名参数”候选。
- 重点不是直接改代码，而是形成一份基于源码事实的候选清单，供下一轮扩参使用。

## Must Know Before Execution

- 这是调研/映射任务，不是实现任务。
- `swat` 与 `swatplus` 必须继续独立，不能建议直接复制 `swat_db.yaml` 条目。
- 只以当前仓库源码和现有 `swatplus_db.yaml` 为事实。
- 判断标准优先看：
- 参数语义是否相近
- 当前 `swatplus` 是否已有同名/近名项
- 是否适合作为下一轮扩参优先项

## Scope

- 阅读：
- `src/hydropilot/models/swat/swat_db.yaml`
- `src/hydropilot/models/swatplus/swatplus_db.yaml`
- 输出：
- 列一个“可优先映射”清单
- 每项写清：
- `swat2012` 参数名
- `swatplus` 当前对应项（如果已有）
- 是否可直接复用“参数概念”
- 为什么适合优先做

## Out Of Scope

- 不修改源码
- 不修改文档
- 不讨论 executable E2E

## Done When

- 至少给出一批高价值候选
- 明确说明：哪些只是“概念可迁移”，哪些已经在 `swatplus` 存在
