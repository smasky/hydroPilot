# TASK T-036

OWNER: w1

## Goal

- 基于 SWAT+ 源码与现有项目事实，整理一份可用于扩展 `swatplus_db.yaml` 的“参数支持清单”。

## Must Know Before Execution

- owner 已明确：`swatplus` 知识库现在应该扩展，不再只停留在最小集。
- 原则是“源码支持 + 文件落点可确认，就入库”。
- 这次任务重点是**支持性层**，不是直接大面积改库。
- 要以源码为事实，不以 Ames_sub1 是否恰好使用某参数为唯一边界。

## Scope

- 优先基于本地已有 SWAT+ 源码、已有调研笔记、现有 `models/swatplus/` 代码，整理：
  - 参数是否真实存在
  - 参数大致属于哪类对象
  - 是否更像适合先入库
  - 是否属于高风险延后项
- 优先关注参数分组：
  - soils
  - channels
  - aquifer
  - reservoir
- 产出应尽量结构化，方便 leader 直接转 backlog。

## Out Of Scope

- 不直接改 `swatplus_db.yaml` 大批量内容。
- 不实现 writer 或 runtime 逻辑。
- 不做 end-to-end 测试接线。

## Done When

- 给出一份按对象/优先级整理的支持清单。
- 清楚区分：
  - 可直接入库
  - 需要进一步确认落点
  - 暂缓项

