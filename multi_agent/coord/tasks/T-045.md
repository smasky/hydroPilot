# TASK T-045

OWNER: w1

## Goal

- 调研并总结：SWAT2012 中最常用的 `output.rch` 站点流量提取思路，在 SWAT+ 中最常见、最等价的用户使用方式是什么。

## Must Know Before Execution

- owner 已明确：这次不是单纯补 `aa / mo / da` 文件名，而是要弄清楚**用户思路映射**。
- 核心问题是：
  - SWAT2012 用户常用 `output.rch + reach/subbasin id + FLOW_OUT`
  - 在 SWAT+ 中通常应该改用哪个对象、哪个文件、哪个变量、哪个 id 心智
- 需要尽量基于真实 SWAT+ 输出体系、官方/源码/项目事实，而不是猜测。

## Scope

- 重点回答：
  - SWAT+ 常见的“站点流量”到底应该从哪类输出取
  - 是 `channel_*`、`hydout_*`、还是别的文件
  - 用户一般是按什么对象 id 来指定站点
  - 与 SWAT2012 `rch` 的关系是“严格对应”还是“近似迁移”
- 最终给 leader 一个可直接转成用户文档/series 设计的结论。

## Out Of Scope

- 不直接改 `swatplus_db.yaml`
- 不做大规模 series 落库
- 不改 runtime / reader / builder

## Done When

- 给出一份清楚的“SWAT2012 rch 流量思路 -> SWAT+ 常用思路”映射说明
- 明确推荐的文件、变量、id 心智

