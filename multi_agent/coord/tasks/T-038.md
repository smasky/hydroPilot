# TASK T-038

OWNER: w3

## Goal

- 基于现有 `swatplus_db.yaml` 骨架，准备首批扩库实施方案，并在证据充分时做小步真实入库。

## Must Know Before Execution

- owner 已明确：`swatplus` 现在应该进入扩库阶段。
- 这次任务可以改 `swatplus_db.yaml`，但前提是证据充分、改动克制。
- 不要等全清单做完再动手；如果已有高把握组，可以先小步入库。
- 仍然要遵守现有知识库边界，不要把运行时/项目特定值塞进库里。

## Scope

- 基于当前 `src/hydropilot/models/swatplus/swatplus_db.yaml`：
  - 设计首批扩库分组的落库方式
  - 如证据充分，先补一小批高价值参数
  - 保持与现有 schema、注释、消费逻辑一致
- 改动要尽量小而真实，优先：
  - soils
  - channels
  - 其他高把握且落点明确的参数

## Out Of Scope

- 不发明新的知识库 schema。
- 不实现 runtime / writer 新逻辑。
- 不强行补全证据不足的参数。

## Done When

- 给出首批扩库实施结果或明确的可落库候选清单。
- 如果已改库，改动应基于真实文件落点和源码支持，不是猜测。

