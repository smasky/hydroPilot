# TASK T-053

OWNER: w1

## Goal

- Implement the core swatplus template migration from fixed_width direct-write to calibration.cal-driven formatted_text writing.

## Must Know Before Execution

- Current swatplus template still returns writerType=fixed_width and builder emits line/start/width direct-write specs.
- General runtime already has formatted_text writer and InstanceInitializer boundary; do not introduce a swatplus-only runtime path.
- swatplus and swat2012 must remain separate templates under models/.

## Scope

- Update src/hydropilot/models/swatplus/template.py, builder.py, and related model-layer code so swatplus physical output targets calibration.cal using general formatted_text writer semantics.
- Use the accepted initialization boundary so calibration.cal header/structure is prepared during instance initialization, not ad hoc during run.
- Keep changes model-layer focused unless a minimal general-layer hook is truly required.
- Add or update tests for expanded swatplus template output shape.

## Out Of Scope

- No swat2012 changes.
- No new runtime special-case branch for swatplus.
- No broad refactor beyond what migration needs.

## Done When

- swatplus template no longer emits fixed_width-style physical entries for migrated parameters.
- generated general config targets calibration.cal via formatted_text-compatible structures.
- tests pass and result is summarized clearly.
