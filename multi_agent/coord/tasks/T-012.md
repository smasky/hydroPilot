# TASK T-012

OWNER: w2

## Goal

- 为 `swatplus` 设计模板级最小骨架，重点是 discovery / builder / series / validate 之间的数据流和 `meta` 形状。
- 输出一份“模板内部接口草图”，供后续真正实现时直接落地。

## Must Know Before Execution

- 这轮任务以设计为主，不修改代码。
- 以现有 `swat` 模板骨架为蓝本，但不要把 SWAT 2012 假设照搬。
- 不引入 `calibration.cal`。
- 第一阶段只考虑 `TxtInOut + txt/csv`。

## Scope

- 阅读并分析：
- `src/hydropilot/models/swat/template.py`
- `src/hydropilot/models/swat/discovery.py`
- `src/hydropilot/models/swat/builder.py`
- `src/hydropilot/models/swat/series.py`
- `src/hydropilot/models/swat/validate.py`
- 输出：
- 建议的 `swatplus meta` 字段结构
- `filter` 第一阶段依赖哪些元数据
- `series` 第一阶段需要哪些输入字段
- 每个模块的输入/输出约定

## Out Of Scope

- 不查更多外部资料。
- 不修改代码。
- 不写伪代码到具体实现细节级别。

## Done When

- 给出一份模板内部接口草图。
- 至少包含：
- `meta` 建议结构
- `discovery -> builder -> series -> validate` 数据流
- 第一阶段最小支持对象集合
- 风险最高的 3 个接口点
