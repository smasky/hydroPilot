# TASK T-014

OWNER: w1

## Goal

- 完成 `swatplus` 接入前的 P0 抽象。
- 去掉 `config/loader.py` 中对 `swat` 的模板特判，改为走 `ModelTemplate` hook。

## Must Know Before Execution

- 这轮是实现任务，不是调研。
- 必须保持现有 `swat` 与 `xaj` 行为不变。
- 不要引入新的中间层，只做最小必要抽象。
- 以当前源码结构为准：`general` 核心不动，模板知识留在 `models/`。

## Scope

- 修改：
- `src/hydropilot/models/base.py`
- `src/hydropilot/config/loader.py`
- `src/hydropilot/models/swat/template.py`
- 如有必要，可补最小测试到现有相关测试文件
- 实现：
- `ModelTemplate.validate()` hook
- `ModelTemplate.translate_exception()` hook
- `loader.py` 改为通过 template hook 做预校验和异常翻译
- `SwatTemplate` 适配新 hook，保持零行为变化

## Out Of Scope

- 不实现 `swatplus`
- 不改 `runtime/params/series` 通用层
- 不做文档改动

## Done When

- `loader.py` 不再写死 `if version == "swat"`
- `swat` 现有行为不变
- 相关测试通过，或说明未能运行的原因
- 列出改动文件和验证结果
