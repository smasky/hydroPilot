# TASK T-072

OWNER: w1

## Goal

- Implement stable soils.sol parser in discovery.py or adjacent model-layer helper. Extract profile ordering, profile names, NLY, and minimal metadata for identity.

## Must Know

- soils.sol format: repeating [profile header + NLY layer rows].
- Profile identity: name-based (e.g., "soil_01", "SONGSAN").
- Need: profile ordering (1-based index), profile names list, NLY per profile.
- Use real Ames_sub1 soils.sol for verification.

## Scope

- Parse soils.sol in discovery.py (or new helper module).
- Return: `n_soils`, `soil_profiles: [{index, name, nly, hyd_grp}]`.
- Handle the two-level format correctly (skip header lines, parse profile header + NLY layer rows).
- Tests: verify against Ames_sub1 (14 profiles, 4 layers each).

## Out Of Scope

- No filter implementation yet.
- No hydro-suffix resolution (that's T-073).
- No calibration ID semantics (that's T-074).

## Done When

- Parser correctly extracts all soil profiles from Ames_sub1.
- Tests pass.
