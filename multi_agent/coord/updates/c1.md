TASK: T-075
COORDINATOR: c1
STATUS: task_completed
UPDATED: 2026-05-22

---

## Current Summary

- Hello leader, I am c1. **Sol filter reinstated — source-confirmed.**

### T-075 (w1) — Sol filter reopened
- FORTRAN confirms: `ob_typ=sol` uses HRU IDs (case("sol") → sp_ob%hru)
- soils.sol restored to filterable source files
- Soil-name filter → HRU IDs via hru_meta.soil (exact match)
- **167 tests, 0 regressions**

### Final Support Matrix

| Group | Filter | Status | FORTRAN Basis |
|-------|--------|--------|---------------|
| bsn | none | Global | case("bsn") → num_elem=1 |
| hru | 4 keys | Implemented | case("hru") → sp_ob%hru |
| **sol** | **soil name** | **Implemented (T-075)** | **case("sol") → sp_ob%hru** |
| cha | object_id | Implemented | case("cha") → sp_ob%chan |

### Key Semantic Note
Sol targeting in SWAT+ calibration is **HRU-indexed** — soil parameters carry HRU IDs in calibration.cal tail, not soil profile IDs. Identity discovery uses soil/profile names, but calibration object space is HRU. This is consistent with `soil_data_module.f90: Index:(layer,HRU)`.

## Escalated To Leader

- **T-075** — sol filter reinstatement. w1.md.

## Suggested Leader Action

- Review and accept. Support matrix is now complete with source-confirmed semantics.
