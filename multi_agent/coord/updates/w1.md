TASK: T-075
WORKER: w1
STATUS: completed
UPDATED: 2026-05-22

---

## Completion Result

I am w1, and I am responsible for T-075.

Reopened soils.sol filter support.  FORTRAN source (`cal_parmchg_read.f90:118` — `case("sol") → sp_ob%hru`) confirms `ob_typ=sol` uses HRU IDs as the object space.  Soil-name filter resolves to HRU IDs via `hru_meta.soil` exact match.  167 pass.

---

## Changes

### `validate.py` — Restored `soils.sol` to `_FILTERABLE_SOURCE_FILES` (line 24)

```python
_FILTERABLE_SOURCE_FILES = {"hydrology.hyd", "soils.sol"}
```

Comment updated with FORTRAN source reference.

### `builder.py` — Restored `soils.sol` to `_HRU_SCOPED_ORIGIN_FILES` (line 22)

```python
_HRU_SCOPED_ORIGIN_FILES = {"hydrology.hyd", "soils.sol"}
```

### Tests — 1 Updated, 1 New

| Test | Status |
|---|---|
| `test_validate_rejects_soil_filter_on_sol_param` → **Renamed to `test_validate_accepts_soil_filter_on_sol_param`** | Now passes validation |
| `test_validate_rejects_soil_filter_on_basin_param` | Unchanged — still rejected |
| `test_soil_filter_on_sol_param_resolves_hrus` | **New** — soil=soil_01 on sol_bd → OBJ_TOT=1, HRU 1 |

## Validation

- Full test suite: **167 passed, 6 skipped** — zero regressions.
- Soil filter on sol param: validation accepted ✓, targeting resolves ✓
- Basin param with filter: still rejected ✓
- HRU/hyd param with filter: unchanged ✓
